{
  "name": "blocks",
  "version": "1.0.0",
  "description": "plugin gulp",
  "main": "gulp.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-watch": "sass --watch --update --style=expanded .",
    "sass-compile": "sass --update --style=expanded ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "postcss": "^8.3.5",
    "sass": "^1.69.0"
  },
  "browserslist": [
    "last 2 versions",
    "IE 11"
  ]
}

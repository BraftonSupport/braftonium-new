{
	"name": "braftonium-blocks",
	"version": "1.0.0",
	"description": "SCSS compilation for Braftonium blocks. Each block keeps its .scss alongside its files (e.g. blocks/banner/banner.scss) and compiles in place to a same-name .css (blocks/banner/banner.css).",
	"private": true,
	"license": "GPL-2.0-or-later",
	"scripts": {
		"sass-watch": "sass --watch . --no-source-map",
		"sass-compile": "sass . --no-source-map"
	},
	"devDependencies": {
		"sass": "~1.77.8"
	}
}

{
  "name": "bones",
  "version": "1.0.0",
  "description": "A Lightweight Wordpress Development Theme",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-watch": "sass --watch --update --style=expanded ./",
    "sass-compile-dev": "sass --style=expanded library/scss:./library/css",
    "sass-compile-prd": "sass --no-source-map --style=compressed library/scss:./library/css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "postcss": "^8.3.5",
    "sass": "^1.69.0"
  },
  "browserslist": [
    "last 2 versions",
    "IE 11"
  ]
}
